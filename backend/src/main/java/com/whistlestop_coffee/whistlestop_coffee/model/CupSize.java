package com.whistlestop_coffee.whistlestop_coffee.model;

// I made this an enum because we only have two sizes right now.
// It's safer than using string inputs.
public enum CupSize {
    REGULAR,
    LARGE
}